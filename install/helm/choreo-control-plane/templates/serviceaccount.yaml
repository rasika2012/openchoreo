apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "choreo-cp.fullname" . }}-controller-manager
  labels:
  {{- include "choreo-cp.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.controllerManager.serviceAccount.annotations | nindent 4 }}
