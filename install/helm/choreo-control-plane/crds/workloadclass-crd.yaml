apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    cert-manager.io/inject-ca-from: choreo-system/choreo-serving-cert
    controller-gen.kubebuilder.io/version: v0.16.4
  name: workloadclasses.core.choreo.dev
spec:
  group: core.choreo.dev
  names:
    kind: WorkloadClass
    listKind: WorkloadClassList
    plural: workloadclasses
    singular: workloadclass
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: WorkloadClass is the Schema for the workloadclasses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkloadClassSpec defines the desired state of WorkloadClass.
            properties:
              manualTask:
                type: object
              scheduledTask:
                type: object
              service:
                properties:
                  deploymentTemplate:
                    x-kubernetes-preserve-unknown-fields: true
                  serviceTemplate:
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              webApplication:
                properties:
                  deploymentTemplate:
                    x-kubernetes-preserve-unknown-fields: true
                  serviceTemplate:
                    x-kubernetes-preserve-unknown-fields: true
                type: object
            type: object
          status:
            description: WorkloadClassStatus defines the observed state of WorkloadClass.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
