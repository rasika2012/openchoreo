import { BoxProps } from '@mui/material';
import { ComponentType } from 'react';
export interface StyledCheckboxProps extends BoxProps {
    disabled?: boolean;
}
export declare const StyledCheckbox: ComponentType<StyledCheckboxProps>;
