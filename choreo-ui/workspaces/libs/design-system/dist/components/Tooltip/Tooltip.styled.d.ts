import { TooltipProps } from '@mui/material';
import { ComponentType } from 'react';
export interface StyledTooltipProps extends TooltipProps {
    disabled?: boolean;
}
export declare const StyledTooltip: ComponentType<StyledTooltipProps>;
