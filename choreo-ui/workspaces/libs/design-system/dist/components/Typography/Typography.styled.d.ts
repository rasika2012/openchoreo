import { TypographyProps } from '@mui/material';
import { ComponentType } from 'react';
export interface StyledTypographyProps {
    monospace?: boolean;
}
export declare const StyledTypography: ComponentType<StyledTypographyProps & TypographyProps>;
