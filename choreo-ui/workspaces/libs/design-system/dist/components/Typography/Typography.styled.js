import { Typography, styled } from '@mui/material';
export const StyledTypography = styled(Typography)(({ monospace }) => ({
    fontFamily: monospace ? 'monospace' : 'inherit',
}));
//# sourceMappingURL=Typography.styled.js.map