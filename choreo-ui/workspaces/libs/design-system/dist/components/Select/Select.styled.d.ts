import { BoxProps } from '@mui/material';
import { ComponentType } from 'react';
export interface StyledSelectProps extends BoxProps {
    disabled?: boolean;
}
export declare const StyledSelect: ComponentType<StyledSelectProps>;
