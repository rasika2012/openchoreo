import { BoxProps } from "@mui/material";
import { ComponentType } from "react";
export interface StyledRotateProps {
    disabled?: boolean;
}
export declare const StyledRotate: ComponentType<StyledRotateProps & BoxProps>;
