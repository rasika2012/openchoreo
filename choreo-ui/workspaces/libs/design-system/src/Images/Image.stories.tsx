import type { Meta, StoryObj } from '@storybook/react';
import { ImageApacheKafkaLogo } from '.';

const meta: Meta<typeof ImageApacheKafkaLogo> = {
  title: 'Choreo DS/Image2',
  component: ImageApacheKafkaLogo,
  tags: ['autodocs'],
};

export default meta;
type Story = StoryObj<typeof ImageApacheKafkaLogo>;

export const Default: Story = {
};

