---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: endpointclasses.openchoreo.dev
spec:
  group: openchoreo.dev
  names:
    kind: EndpointClass
    listKind: EndpointClassList
    plural: endpointclasses
    singular: endpointclass
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EndpointClass is the Schema for the endpointclasses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EndpointClassSpec defines the desired state of EndpointClass.
            properties:
              grpcPolicy:
                description: GRPCEndpointPolicy defines gRPC-specific endpoint policies
                  (placeholder for future implementation)
                type: object
              restPolicy:
                description: RESTEndpointPolicy defines REST-specific endpoint policies
                type: object
            type: object
          status:
            description: EndpointClassStatus defines the observed state of EndpointClass.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
