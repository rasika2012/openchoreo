---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: workloadreleases.core.choreo.dev
spec:
  group: core.choreo.dev
  names:
    kind: WorkloadRelease
    listKind: WorkloadReleaseList
    plural: workloadreleases
    singular: workloadrelease
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: WorkloadRelease is the Schema for the workloadreleases API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkloadReleaseSpec defines the desired state of WorkloadRelease.
            properties:
              environmentName:
                minLength: 1
                type: string
              owner:
                properties:
                  componentName:
                    minLength: 1
                    type: string
                  projectName:
                    minLength: 1
                    type: string
                required:
                - componentName
                - projectName
                type: object
              resources:
                description: |-
                  Scalable resource template approach (KRO-inspired)
                  Supports any Kubernetes resource type including HPA, PDB, NetworkPolicy, CRDs, etc. that can
                  be applied to the data plane.
                items:
                  description: Resource defines a Kubernetes resource template that
                    can be applied to the data plane.
                  properties:
                    id:
                      description: Unique identifier for the resource
                      minLength: 1
                      type: string
                    object:
                      description: Object contains the complete Kubernetes resource
                        definition
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - id
                  - object
                  type: object
                type: array
              type:
                description: WorkloadType defines how the workload is deployed.
                type: string
            required:
            - environmentName
            - owner
            - type
            type: object
          status:
            description: WorkloadReleaseStatus defines the observed state of WorkloadRelease.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
