---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: endpoints.core.choreo.dev
spec:
  group: core.choreo.dev
  names:
    kind: Endpoint
    listKind: EndpointList
    plural: endpoints
    singular: endpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].message
      name: url
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Endpoint is the Schema for the endpoints API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EndpointSpec defines the desired state of Endpoint
            properties:
              apiSettings:
                description: Configuration parameters related to the managed endpoint
                properties:
                  authorizationHeader:
                    type: string
                  backendJwt:
                    description: BackendJWTConfig defines JWT configuration for backend
                      services
                    properties:
                      configuration:
                        description: BackendJWTConfigDetails contains the detailed
                          JWT configuration
                        properties:
                          audiences:
                            items:
                              type: string
                            type: array
                        required:
                        - audiences
                        type: object
                      enabled:
                        type: boolean
                    required:
                    - configuration
                    - enabled
                    type: object
                  operationPolicies:
                    items:
                      description: OperationPolicy defines authentication policy for
                        an API operation
                      properties:
                        authenticationType:
                          type: string
                        target:
                          type: string
                      required:
                      - authenticationType
                      - target
                      type: object
                    type: array
                  organizationVisibilityConfigurations:
                    description: VisibilityConfigurations defines configurations for
                      different visibility levels
                    properties:
                      cors:
                        description: CORSConfig defines Cross-Origin Resource Sharing
                          configuration
                        properties:
                          allowHeaders:
                            items:
                              type: string
                            type: array
                          allowMethods:
                            items:
                              type: string
                            type: array
                          allowOrigins:
                            items:
                              type: string
                            type: array
                          enabled:
                            type: boolean
                          exposeHeaders:
                            items:
                              type: string
                            type: array
                        required:
                        - allowHeaders
                        - allowMethods
                        - allowOrigins
                        - enabled
                        - exposeHeaders
                        type: object
                      rateLimit:
                        description: RateLimitConfig defines rate limiting configuration
                        properties:
                          tier:
                            type: string
                        required:
                        - tier
                        type: object
                    type: object
                  publicVisibilityConfigurations:
                    description: VisibilityConfigurations defines configurations for
                      different visibility levels
                    properties:
                      cors:
                        description: CORSConfig defines Cross-Origin Resource Sharing
                          configuration
                        properties:
                          allowHeaders:
                            items:
                              type: string
                            type: array
                          allowMethods:
                            items:
                              type: string
                            type: array
                          allowOrigins:
                            items:
                              type: string
                            type: array
                          enabled:
                            type: boolean
                          exposeHeaders:
                            items:
                              type: string
                            type: array
                        required:
                        - allowHeaders
                        - allowMethods
                        - allowOrigins
                        - enabled
                        - exposeHeaders
                        type: object
                      rateLimit:
                        description: RateLimitConfig defines rate limiting configuration
                        properties:
                          tier:
                            type: string
                        required:
                        - tier
                        type: object
                    type: object
                  securitySchemes:
                    items:
                      type: string
                    type: array
                type: object
              networkVisibilities:
                description: Network visibility levels that the endpoint is exposed
                items:
                  type: string
                type: array
              schema:
                description: Schema of the endpoint if available
                properties:
                  content:
                    description: Inline content of the schema
                    type: string
                  filePath:
                    description: File path of the schema relative to the component
                      source code
                    type: string
                type: object
              service:
                description: Configuration of the upstream service
                properties:
                  basePath:
                    description: Base path of the upstream service
                    type: string
                  port:
                    description: Port of the upstream service
                    format: int32
                    type: integer
                  url:
                    description: URL of the upstream service
                    type: string
                required:
                - port
                type: object
              type:
                description: Type indicates the protocol of the endpoint
                enum:
                - HTTP
                - REST
                - gRPC
                - GraphQL
                - Websocket
                - TCP
                - UDP
                type: string
              webappGatewaySettings:
                additionalProperties:
                  type: string
                description: Configuration parameters related to the webapp gateway
                type: object
            required:
            - service
            - type
            type: object
          status:
            description: EndpointStatus defines the observed state of Endpoint
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
