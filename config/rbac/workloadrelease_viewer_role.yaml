# permissions for end users to view workloadreleases.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: choreo
    app.kubernetes.io/managed-by: kustomize
  name: workloadrelease-viewer-role
rules:
- apiGroups:
  - core.choreo.dev
  resources:
  - workloadreleases
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - core.choreo.dev
  resources:
  - workloadreleases/status
  verbs:
  - get
