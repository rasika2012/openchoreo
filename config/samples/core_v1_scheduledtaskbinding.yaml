apiVersion: core.choreo.dev/v1
kind: ScheduledTaskBinding
metadata:
  labels:
    app.kubernetes.io/name: choreo
    app.kubernetes.io/managed-by: kustomize
  name: scheduledtaskbinding-sample
spec:
  # TODO(user): Add fields here
