apiVersion: core.choreo.dev/v1
kind: EndpointClass
metadata:
  labels:
    app.kubernetes.io/name: choreo
    app.kubernetes.io/managed-by: kustomize
  name: endpointclass-sample
spec:
  # TODO(user): Add fields here
