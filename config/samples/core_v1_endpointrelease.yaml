apiVersion: core.choreo.dev/v1
kind: EndpointRelease
metadata:
  labels:
    app.kubernetes.io/name: choreo
    app.kubernetes.io/managed-by: kustomize
  name: endpointrelease-sample
spec:
  # TODO(user): Add fields here
