apiVersion: core.choreo.dev/v1
kind: ScheduledTaskRelease
metadata:
  labels:
    app.kubernetes.io/name: choreo
    app.kubernetes.io/managed-by: kustomize
  name: scheduledtaskrelease-sample
spec:
  # TODO(user): Add fields here
