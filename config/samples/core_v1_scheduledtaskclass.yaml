apiVersion: core.choreo.dev/v1
kind: ScheduledTaskClass
metadata:
  labels:
    app.kubernetes.io/name: choreo
    app.kubernetes.io/managed-by: kustomize
  name: scheduledtaskclass-sample
spec:
  # TODO(user): Add fields here
